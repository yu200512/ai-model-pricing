===TITLES===
1. A100 的真正主角：Tensor Core 与 MIG
2. Ampere 架构深挖：Tensor Core 三代演进与 MIG 多实例设计
3. NVIDIA A100 为什么能打：TC 加速、MIG 切分与选型逻辑

===DIGEST===
A100 的灵魂不在制程，在于 Tensor Core 演进与 MIG 硬件级切分的协同。

===CONTENT===

2020 年 5 月，NVIDIA 基于 Ampere 架构推出 A100 GPU，一经面世便在 AI 训练与高性能计算领域横扫各项基准测试。作为数据中心的主流加速器，很多人对它的认知止步于"显存大"和"算力强"。但如果只盯着硬件参数表，会错过 Ampere 真正值得深挖的设计意图：Tensor Core 的持续演进，以及 MIG 多实例切分机制带来的资源利用率革新。这两者共同构成了 A100 在生产环境中长盛不衰的底层逻辑。

Ampere 架构采用台积电 N7 (TSMC N7) 工艺，集成约 540 亿晶体管，80GB SXM 规格峰值显存带宽接近 2 TB/s，支持 NVLink 高速互联，PCIe 版本通过 PCIe 4.0 x16 与主机通信。这些指标放在 2020 年确实领先，但工艺和带宽本身并非 Ampere 独有的护城河——真正让这款芯片区别于前代产品的，是下面要展开的 Tensor Core 架构升级。

---

**Tensor Core 的划时代起点**

2017 年，NVIDIA 在 Volta 架构中首次引入 Tensor Core，将矩阵乘加操作做成了专用硬件单元。在此之前，GPU 处理矩阵运算依赖通用 CUDA Core，通过 cuBLAS 或 cuDNN 等库将 GEMM 拆解为大量细粒度乘加操作，消耗大量时钟周期。Tensor Core 一个时钟周期即可完成一次完整的矩阵乘法累加，无需拆分到数千个 CUDA Core 上排队等待。这种硬件级加速在速度上是一次质的飞跃，也为后续深度学习框架的性能天花板奠定了基础。

---

**Ampere 新格式：TF32 与 BF16**

Ampere 的第三代 Tensor Core 新增了对 TF32 和 BF16 两种数据格式的原生支持。TF32（Tensor Float 32）是 19 位精度浮点格式，设计初衷是在训练场景中兼顾数值稳定性与计算效率；BF16（Brain Float 16）拥有与 FP32 相同的指数位宽，数值范围更宽，更适合大模型梯度更新。两种格式面向不同场景互补存在，共同目标是在深度学习训练和推理中以更少的计算量换取接近 FP32 的精度。

与数据格式同步进化的还有稀疏矩阵计算加速能力。稀疏矩阵指矩阵中大量元素为零，在真实深度学习模型中权重矩阵往往高度稀疏——大量零元素参与传统运算时，纯算力浪费在零与零的乘加上。Ampere 的解法是硬件识别 2:4 结构化稀疏模式（即每 4 个元素中最多 2 个非零值），直接跳过零元素位置，用压缩后的非零数据完成计算，在同样多的晶体管上实现最高 2 倍有效吞吐量提升，同时降低能耗。这一能力对已完成模型剪枝的场景尤为有价值。

---

**TF32 在 A100 上的实际工作路径**

在 A100 上，Tensor Core 使用 TF32 作为计算精度完成矩阵乘加，存储在显存中的模型权重、激活值等状态则保持 FP32 精度。TF32 仅作用于计算过程，FP32 存储保证训练过程中精度累积误差不会导致数值不稳定。实际使用时，PyTorch 会自动识别模型中的 FP32 GEMM 操作并路由到 Tensor Core 路径执行，无需开发者手动指定混合精度或修改任何代码。在 PyTorch 1.7 及以上版本中，只要使用支持 Tensor Core 的 CUDA 版本，A100 的 TF32 加速便自动生效。

---

**第三代 Tensor Core 三大新特性**

NVIDIA 官方数据显示，相比 V100，A100 的第三代 Tensor Core 在三个维度上实现显著提升。TF32 格式配合 PyTorch 自动路由，最高提速 20 倍，这是 NVIDIA 在发布会上重点宣传的数字。结构化稀疏采用 2:4 模式，最高再增 2 倍吞吐量，两条加速路径叠加效果可观。第三项常被忽视：A100 原生支持符合 IEEE 754 标准的 FP64 双精度计算，相比 V100 提升 2.5 倍，使其在需要高精度数值计算的科学计算场景中同样具备竞争力。

---

**MIG：硬件级多实例切分**

如果说 Tensor Core 代表单点算力的极致，MIG（Multi-Instance GPU）则是资源利用率层面的关键创新。MIG 允许将单张 A100 在硬件层面切分为最多 7 个独立 GPU 实例，每个实例独占显存子分区、计算单元和缓存，彼此之间硬件级隔离，消除了虚拟化开销，也为多任务场景下的性能可预测性提供保障。

生产环境中，MIG 的价值体现在几个典型场景。单卡多任务并行是最常见的用法——同一台服务器上同时运行多个推理服务时，每个服务独占一个实例，避免了资源竞争和服务间的调度干扰。多租户云环境中，MIG 提供更细粒度的算力分配，运营商可按实例数量而非整卡出售资源，降低用户使用成本。由于实例间完全隔离，一个实例的应用崩溃或内存溢出不会扩散到其他实例，实现了天然的故障隔离。这些特性让 MIG 成为云服务商和企业内部集群管理的重要工具。

---

**80GB PCIe 与 SXM：选型的一点心得**

A100 提供 80GB PCIe 和 80GB SXM 两种规格，纯计算性能几乎无差异，主要区别在多卡互联带宽上。SXM 版本通过 NVLink 高速互联，多卡并行时带宽远高于 PCIe，适合大规模多卡训练场景；PCIe 版本通过 PCIe 4.0 x16 通信，带宽受限于标准 PCIe 通道规格。我选的是 80GB PCIe 版本，理由很实际：现有服务器已支持 PCIe 4.0，无需为 SXM 专用主板额外投入，部署便利性在当时是更务实的选择。如果计划组建多卡集群或使用 DGX 系统，SXM 的互联效率优势就值得纳入考量了。

---

**写在最后**

回顾 A100 的整个设计，NVIDIA 的重心并不在制程工艺的炫耀上——真正的护城河是 Tensor Core 的持续演进和 MIG 的资源切分能力，两者共同构成了 Ampere 架构在 AI 时代的高利用率与高适应性。下一次我们将从 SM 内部结构入手，聊聊 CUDA Core 与 Tensor Core 的协作细节，以及异步数据拷贝如何绕过显存带宽瓶颈，最后动手写一个 Triton kernel 体验低级别编程的真实手感。

📌 参考：NVIDIA A100 数据表（https://www.nvidia.com/en-us/data-center/a100/）
